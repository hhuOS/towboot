cargo-features = ["per-package-target"]

[package]
name = "towboot"
version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
edition = "2024"
default-target = "i686-unknown-uefi"

[[bin]]
# integration tests are in the tests crate
name = "towboot"
test = false
doctest = false
bench = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
uefi = { version = "0.36", features = ["alloc", "global_allocator", "logger", "panic_handler"] }
acpi = { version = "6.0", default-features = false }
dmidecode = { version = "1.0", default-features = false }
x86 =  "0.52"

log = { version = "0.4", default-features = false }

toml = { version = "0.9", default-features = false, features = ["parse", "serde"] }
multiboot12 = { path = "../multiboot12" }
goblin = { version = "0.10", default-features = false, features = ["elf32", "elf64", "endian_fd"] }
scroll = { version = "0.13", default-features = false }

towboot_config = { path = "../towboot_config" }

[build-dependencies]
built = { version = "0.8", features = ["git2"] }
